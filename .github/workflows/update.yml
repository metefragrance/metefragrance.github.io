name: Update Products

on:
  repository_dispatch:
    types: [add, delete]

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          ref: main          # 🔴 ÇOK ÖNEMLİ
          persist-credentials: true

      - name: Debug info
        run: |
          echo "BRANCH:"
          git branch
          echo "STATUS BEFORE:"
          git status

      - name: Run update script
        run: node update.js

      - name: Status after update
        run: |
          echo "STATUS AFTER:"
          git status
          cat index.html | tail -n 20

      - name: Commit and push
        run: |
          git config user.name "mete-bot"
          git config user.email "mete-bot@users.noreply.github.com"
          git add index.html
          git commit -m "ürün güncellendi $(date +%s)"
          git push origin main
